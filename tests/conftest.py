"""Shared test fixtures.

Add project-wide fixtures here. They will be automatically available
to all tests without importing.

Example:
    @pytest.fixture
    def sample_config() -> Config:
        return Config(seed=42)
"""
