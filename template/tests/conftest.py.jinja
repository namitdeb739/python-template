"""Shared test fixtures.

Fixtures defined here are automatically available to all tests without
importing. Use these as starting points and add project-specific fixtures
as the codebase grows.
"""

from __future__ import annotations

from typing import TYPE_CHECKING

import pytest

from {{ package_name }}.config import Config

if TYPE_CHECKING:
{%- if use_iot %}
    from collections.abc import Generator
{%- endif %}
    from pathlib import Path
{%- if use_iot %}
    from unittest.mock import MagicMock
{%- endif %}


@pytest.fixture
def sample_config(tmp_path: Path) -> Config:
    """A Config instance with temp directories for isolated tests."""
    return Config(
        data_dir=tmp_path / "data",
        output_dir=tmp_path / "output",
        seed=42,
    )


@pytest.fixture
def data_dir(tmp_path: Path) -> Path:
    """A temporary data directory that exists on disk."""
    d = tmp_path / "data"
    d.mkdir()
    return d
{%- if use_iot %}


# ---------------------------------------------------------------------------
# IoT fixtures
# ---------------------------------------------------------------------------


@pytest.fixture
def mock_serial() -> Generator[MagicMock, None, None]:
    """Mock serial.Serial so serial tests run without hardware attached."""
    from unittest.mock import MagicMock as _MagicMock
    from unittest.mock import patch

    def _configure(mock: MagicMock) -> MagicMock:
        mock.readline.return_value = b"OK\r\n"
        mock.read.return_value = b"\x00"
        mock.write.return_value = 4
        mock.is_open = True
        return mock

    try:
        import serial as _serial  # noqa: F401
    except ImportError:
        yield _configure(_MagicMock())
        return

    with patch("serial.Serial") as mock_cls:
        instance = mock_cls.return_value.__enter__.return_value = mock_cls.return_value
        yield _configure(instance)


@pytest.fixture(autouse=True)
def mock_gpio() -> Generator[None, None, None]:
    """Replace gpiozero pin factory with MockFactory for all tests.

    This means GPIO tests run in CI without any physical hardware.
    To test with real hardware, remove this fixture or override it locally.
    """
    try:
        from gpiozero import Device
        from gpiozero.pins.mock import MockFactory

        original = Device.pin_factory
        Device.pin_factory = MockFactory()
        yield
        Device.pin_factory = original
    except ImportError:
        yield  # gpiozero not installed; skip silently
{%- endif %}
