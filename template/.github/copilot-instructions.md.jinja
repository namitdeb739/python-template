# Copilot Instructions

## Project Overview

This is a Python project using **uv** for dependency management and **just** as a task runner. Source lives under `src/{{ package_name }}/` (src layout). Configuration is via typed dataclasses in `src/{{ package_name }}/config.py`.

## Key Commands

```bash
just setup       # Install deps{% if security != 'none' %} + pre-commit hooks{% endif %}

just check       # lint{% if use_typecheck %} + typecheck{% endif %} + test (full CI mirror)
just lint        # ruff check + format check
just fix         # auto-fix lint and formatting
{%- if use_typecheck %}
just typecheck   # mypy strict
{%- endif %}
just test        # pytest
{%- if testing != 'minimal' %}
just coverage    # pytest with coverage report{% if testing == 'full' %} (80% threshold){% endif %}

{%- endif %}
{%- if use_docs %}
just docs        # mkdocs serve (local preview)
{%- endif %}
just release patch|minor|major  # bump version, tag, push
```

## Code Style

- **Formatter/linter**: ruff (line length 88, pyupgrade, bugbear, simplify rules enforced)
{%- if use_typecheck %}
- **Types**: mypy strict — all public functions must be fully annotated
{%- endif %}
- **Imports**: isort via ruff; stdlib → third-party → first-party order
- **Commits**: conventional commits enforced (`feat:`, `fix:`, `chore:`, `refactor:`, `docs:`, `test:`, `ci:`)

## Architecture Patterns

- **Config**: `dataclasses.dataclass` in `config.py` — no Pydantic, no env parsing at import time. `__post_init__` converts `Path` fields.
- **Entry point**: `src/{{ package_name }}/main.py` — CLI wired via `[project.scripts]` in `pyproject.toml`
- **Tests**: pytest{% if testing != 'minimal' %}, fixtures in `tests/conftest.py`, parametrize for data-driven cases{% endif %}

{%- if use_docs %}
- **Docs**: MkDocs Material + mkdocstrings — docstrings drive the API reference page
{%- endif %}
- Keep reusable production logic in `src/{{ package_name }}/`, not in notebooks
{%- if use_ml %}
- Keep exploratory work in `notebooks/` and standalone utilities in `scripts/`
- Treat `data/` as gitignored/DVC-ready data storage
{%- endif %}

## Adding Dependencies

```bash
uv add <package>           # runtime dependency
uv add --dev <package>     # dev/test dependency
{%- if use_ml %}
uv add --optional ml <package>  # optional ML extras
{%- endif %}
{%- if use_iot %}
uv add --optional iot <package> # optional IoT extras
{%- endif %}
```

Always commit `uv.lock` after adding dependencies.

## Testing Conventions

- Fixtures go in `conftest.py` — never import them explicitly in test files
- Use `tmp_path` (built-in) for temporary file/directory fixtures
- Parametrize data-driven tests with `@pytest.mark.parametrize`
{%- if testing == 'full' %}
- Coverage threshold: 80% minimum (enforced in `just coverage`)
{%- endif %}
{% if ci_github %}

## CI Workflows

| Workflow | Trigger | Checks |
|----------|---------|--------|
| `ci.yml` | push/PR to main | lint,{% if use_typecheck %} typecheck,{% endif %} test (3.11–3.13){% if security != 'none' %}, audit{% endif %} |
{%- if security == 'full' %}
| `codeql.yml` | push/PR + weekly | static security analysis |
{%- endif %}
| `commit-lint.yml` | PR | conventional commit format |
{%- if use_docs %}
| `docs.yml` | push to main | build and deploy MkDocs to GitHub Pages |
{%- endif %}
| `publish.yml` | version tag push | build + publish to PyPI |
{% endif %}
{% if use_docs %}

## Docs Links

- Project overview and command quickstart: `README.md`
- Development workflow and coding expectations: `docs/getting-started.md`, `docs/contributing.md`
{%- if ci_github %}
- CI/CD and security workflow details: `docs/ci-cd.md`
{%- endif %}
{%- if use_docker != 'none' %}
- Container workflows: `docs/docker.md`
{%- endif %}
{%- if use_ml %}
- ML and data workflow details: `docs/ml-setup.md`
{%- endif %}
- Release process and versioning details: `docs/releasing.md`
{% endif %}

{% if use_iot %}
## IoT / Embedded Development

- **Drivers live in** `src/{{ package_name }}/drivers/` — serial (`serial.py`), GPIO (`gpio.py`), MQTT (`mqtt.py`)
- **Device config**: `config/device.yaml` (gitignored) — copy from `config/device.yaml.example`
- **Abstractions**: all hardware access goes through `typing.Protocol` interfaces — never import `serial`/`gpiozero`/`paho` directly in business logic
- **Testing without hardware**: use `mock_serial` and `mock_gpio` fixtures from `tests/conftest.py` (autouse for GPIO); MQTT tests require a local Mosquitto broker or skip gracefully

```bash
just deploy <host>   # rsync src/ + restart systemd service (or docker pull + compose up if use_docker)
```

- **paho-mqtt**: import as `paho.mqtt.client` and always construct with `CallbackAPIVersion.VERSION2` (paho 2.x+)
- **GPIO**: use `gpiozero` — `Device.pin_factory = MockFactory()` in test fixtures for CI-safe testing
{% endif %}

## What NOT to Do

- Do not bypass `uv` with `pip install` — always use `uv add` or `uv run`
- Do not add `print()` debug statements to library code — use `logging`
{%- if use_ml %}
- Do not commit `.env` files — use `.env.example` as the template
{%- endif %}
- Do not add secrets or credentials to any tracked file
